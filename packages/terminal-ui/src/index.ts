// @ai-rpg-engine/terminal-ui — terminal renderer and input

export {
  renderScene,
  renderEventLog,
  renderActions,
  renderDialogue,
  renderFullScreen,
  parseActionSelection,
  parseTextInput,
  humanizeStateId,
  DIALOGUE_LOOKBACK,
  EVENT_LOG_LOOKBACK,
} from './renderer.js';
